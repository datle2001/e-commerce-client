export enum LoginState {
  LOGGING_IN,
  LOGGED_IN,
  NOT_LOGGED_IN
}

export enum OrderState {
  NOT_SUBMIITED,
  SUBMITTING,
  SUBMITTED
}

export enum ToastType {
  SUCCESS = 'Success',
  ERROR = 'Error',
  WARNING = 'Warning'
}

export enum ScreenSize {
  MINI,
  SMALL,
  NORMAL,
}